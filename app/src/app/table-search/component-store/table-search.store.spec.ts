import { TableSearchStore } from './table-search.store';

describe('TableSearchStore', () => {
  const componentStore = new TableSearchStore();

  it('should be created', () => {
    expect(componentStore).toBeTruthy();
  });
});
