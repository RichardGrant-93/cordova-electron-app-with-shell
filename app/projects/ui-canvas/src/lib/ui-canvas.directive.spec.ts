import { UiCanvasDirective } from './ui-canvas.directive';

describe('UiCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new UiCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
