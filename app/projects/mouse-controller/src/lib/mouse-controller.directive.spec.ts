import { MouseControllerDirective } from './mouse-controller.directive';

describe('MouseControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseControllerDirective();
    expect(directive).toBeTruthy();
  });
});
